  # Verify signature files.
  # If `false` then `publicKey` may be ommitted.
  # Defaults to `true`.
  # icon:dot-circle[]
  verify = false

  # Sign files.
  # Defaults to `true`.
  # icon:dot-circle[]
  files = true

  # Sign distribution artifacts.
  # Defaults to `true`.
  # icon:dot-circle[]
  artifacts = true

  # Sign checksum files.
  # Defaults to `true`.
  # icon:dot-circle[]
  checksums = true